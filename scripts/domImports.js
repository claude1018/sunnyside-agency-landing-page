const menu = document.querySelector('.hamburger');
const imageTransformElement = document.querySelector(
  'figure.expect.image.one img'
);
const imageStandOutElement = document.querySelector(
  'figure.expect.image.two img'
);

const imageGalleryMilkBottlesElement =
  document.querySelector('.gallery.image.one');
const imageGalleryOrangeElement = document.querySelector('.gallery.image.two');
const imageGalleryConeElement = document.querySelector('.gallery.image.three');
const imageGallerySugarCubesElement = document.querySelector(
  '.gallery.image.four'
);
